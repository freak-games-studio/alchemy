export const threshold = 120
export const innerWidth = window.innerWidth - threshold
export const innerHeight = window.innerHeight - threshold
export const elementWidth = 60
export const elementHeight = 40
